# presenter_mode.conf

# External display / projector — mirrored from laptop, safe fallback resolution

# Laptop screen — mirrored by pointing it at the same position and resolution
monitor = eDP-1, 2560x1440, 0x0, 1.25
monitor = , preferred, auto, 1, mirror, eDP-1

# Fallback: catch any other connected output and disable it
